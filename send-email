<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <title>Gửi yêu cầu truy cập</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      padding: 30px;
    }

    h3 {
      font-size: 24px;
    }

    #folderInfo {
      margin: 10px 0;
      font-size: 20px;
      font-weight: bold;
      color: #333;
    }

    input[type="email"] {
      padding: 10px;
      width: 300px;
      font-size: 16px;
    }

    button {
      padding: 10px 20px;
      font-size: 16px;
      margin-left: 10px;
      cursor: pointer;
    }

    #status {
      margin-top: 15px;
      font-weight: bold;
    }
  </style>
</head>
<body>
  <h3>Gửi yêu cầu truy cập</h3>
  <p id="folderInfo">Đang tải thông tin thư mục...</p>

  <input type="email" id="email" placeholder="Nhập email..." />
  <button onclick="sendEmail()">Gửi</button>
  <p id="status"></p>

  <script src="https://cdn.jsdelivr.net/npm/@emailjs/browser@3/dist/email.min.js"></script>
  <script>
    // Khởi tạo EmailJS
    emailjs.init("bNyTDo7SAT7JYMWH1");

    // Lấy tên thư mục từ localStorage
    const folderName = localStorage.getItem("selected_folder_name") || "Không rõ thư mục";
    document.getElementById("folderInfo").textContent = `📁 Thư mục đang chọn: ${folderName}`;

    // Lấy ID thiết bị
    function getDeviceId() {
      let id = localStorage.getItem("device_id");
      if (!id) {
        id = crypto.randomUUID();
        localStorage.setItem("device_id", id);
      }
      return id;
    }

    // Gửi email
    function sendEmail() {
      const email = document.getElementById("email").value.trim();
      const deviceId = getDeviceId();
      const statusEl = document.getElementById("status");

      if (!email) {
        alert("Vui lòng nhập email.");
        return;
      }

      emailjs.send("0566681993", "0376425426", {
        email: email,
        device_id: deviceId,
        folder_name: folderName
      }).then(() => {
        statusEl.innerText = "✅ Đã gửi yêu cầu thành công!";
      }).catch((error) => {
        console.error("Lỗi gửi email:", error);
        statusEl.innerText = "❌ Gửi thất bại. Kiểm tra cấu hình EmailJS.";
      });
    }
  </script>
</body>
</html>
